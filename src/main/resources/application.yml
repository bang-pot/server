spring:
  application:
    name: bangpot-backend
  profiles:
    default: local
  jpa:
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  datasource:
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: ${BANGPOT_REDIS_HOST:localhost}
      port: ${BANGPOT_REDIS_PORT:6379}
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 6MB

  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: ${KAKAO_CLIENT_ID}
            client-secret: ${KAKAO_CLIENT_SECRET}
            client-authentication-method: ${KAKAO_CLIENT_AUTHENTICATION_METHOD:client_secret_post}
            redirect-uri: ${KAKAO_REDIRECT_URI}
            authorization-grant-type: authorization_code
            client-name: Kakao
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

bangpot:
  auth:
    required-terms-version: ${BANGPOT_AUTH_REQUIRED_TERMS_VERSION}
    frontend:
      base-url: ${BANGPOT_AUTH_FRONTEND_BASE_URL}
    jwt:
      issuer: ${BANGPOT_AUTH_JWT_ISSUER:bangpot}
      secret: ${BANGPOT_AUTH_JWT_SECRET}
      cookie-name: ${BANGPOT_AUTH_JWT_COOKIE_NAME:access_token}
      access-token-validity-seconds: ${BANGPOT_AUTH_JWT_ACCESS_TOKEN_VALIDITY_SECONDS:604800}
      secure-cookie: ${BANGPOT_AUTH_JWT_SECURE_COOKIE:true}
  ops:
    environment-name: ${BANGPOT_OPS_ENVIRONMENT_NAME:default}
    slack:
      all-log-webhook-url: ${BANGPOT_OPS_SLACK_ALL_LOG_WEBHOOK_URL:}
      error-log-webhook-url: ${BANGPOT_OPS_SLACK_ERROR_LOG_WEBHOOK_URL:}
  storage:
    public-base-url: ${BANGPOT_STORAGE_PUBLIC_BASE_URL:}
    s3:
      bucket: ${BANGPOT_STORAGE_S3_BUCKET:}
      region: ${BANGPOT_STORAGE_S3_REGION:${AWS_REGION:ap-northeast-2}}
      access-key: ${BANGPOT_STORAGE_S3_ACCESS_KEY:}
      secret-key: ${BANGPOT_STORAGE_S3_SECRET_KEY:}
  cache:
    redis:
      enabled: ${BANGPOT_CACHE_REDIS_ENABLED:false}
      default-ttl: ${BANGPOT_CACHE_REDIS_DEFAULT_TTL:5m}
      explore-theme-search-ttl: ${BANGPOT_CACHE_REDIS_EXPLORE_THEME_SEARCH_TTL:60s}
      home-anonymous-ttl: ${BANGPOT_CACHE_REDIS_HOME_ANONYMOUS_TTL:60s}

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
      show-details: always
      group:
        liveness:
          include: ping
        readiness:
          include: db

server:
  port: ${SERVER_PORT:8080}
